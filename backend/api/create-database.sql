-- Create database liora if not exists
CREATE DATABASE IF NOT EXISTS liora CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;

-- Show databases to verify
SHOW DATABASES LIKE 'liora';

-- Select database
USE liora;

-- Show tables (should be empty initially)
SHOW TABLES;
